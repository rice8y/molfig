[package]
name = "molfig"
version = "0.1.0"
edition = "2021"
description = "PDB/mmCIF to OBJ/STL/PLY converter for Typst and maquette"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
